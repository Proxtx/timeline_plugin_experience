[package]
name = "timeline_plugin_experience_server"
version = "0.1.0"
edition = "2021"

[dependencies]
server_api = { path = "../../../server_api/" }
rocket = { version = "0.5.1", features = ["json"] }
serde = { version = "1.0.215", features = ["derive"] }
